#include "main.h"

/**
 * _isupper - checks if a letter is upper
 * @c: value
 * Return: 0 || 1
 */
int _isupper(int c)
{
	char first;
	char last;

	if ((c >= first) && (c <= last))
		return (1);
	else
		return (0);
}
