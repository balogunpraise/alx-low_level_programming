#include "main.h"
#include<stdio.h>

/**
 * print_times_table - print the times table
 * @n: limit
 * Return: 0
 */
void print_times_table(int n)
{
	int row, column, number;

	if (n >= 0 && n <= 15)
	{
		while (

